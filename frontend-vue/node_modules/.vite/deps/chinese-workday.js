import "./chunk-UXIASGQL.js";

// node_modules/chinese-workday/src/index.js
var LRUCache = class {
  constructor(maxSize = 1e3) {
    this.cache = /* @__PURE__ */ new Map();
    this.maxSize = maxSize;
    this.hits = 0;
    this.misses = 0;
  }
  get(key) {
    const value = this.cache.get(key);
    if (value !== void 0) {
      this.hits++;
      this.cache.delete(key);
      this.cache.set(key, value);
      return value;
    }
    this.misses++;
    return void 0;
  }
  set(key, value) {
    if (this.cache.has(key)) {
      this.cache.delete(key);
    }
    this.cache.set(key, value);
    if (this.cache.size > this.maxSize) {
      const firstKey = this.cache.keys().next().value;
      this.cache.delete(firstKey);
    }
  }
  getStats() {
    return {
      size: this.cache.size,
      maxSize: this.maxSize,
      hits: this.hits,
      misses: this.misses,
      hitRate: this.hits / (this.hits + this.misses) * 100
    };
  }
};
var dateCache = new LRUCache(1e3);
var HOLIDAYS = {
  "2011-01-01": "元旦",
  "2011-01-02": "元旦",
  "2011-01-03": "元旦",
  "2011-02-02": "春节",
  "2011-02-03": "春节",
  "2011-02-04": "春节",
  "2011-02-05": "春节",
  "2011-02-06": "春节",
  "2011-02-07": "春节",
  "2011-02-08": "春节",
  "2011-04-03": "清明节",
  "2011-04-04": "清明节",
  "2011-04-05": "清明节",
  "2011-04-30": "劳动节",
  "2011-05-01": "劳动节",
  "2011-05-02": "劳动节",
  "2011-06-04": "端午节",
  "2011-06-05": "端午节",
  "2011-06-06": "端午节",
  "2011-09-10": "中秋节",
  "2011-09-11": "中秋节",
  "2011-09-12": "中秋节",
  "2011-10-01": "国庆节",
  "2011-10-02": "国庆节",
  "2011-10-03": "国庆节",
  "2011-10-04": "国庆节",
  "2011-10-05": "国庆节",
  "2011-10-06": "国庆节",
  "2011-10-07": "国庆节",
  "2012-01-01": "元旦",
  "2012-01-02": "元旦",
  "2012-01-03": "元旦",
  "2012-01-22": "春节",
  "2012-01-23": "春节",
  "2012-01-24": "春节",
  "2012-01-25": "春节",
  "2012-01-26": "春节",
  "2012-01-27": "春节",
  "2012-01-28": "春节",
  "2012-04-02": "清明节",
  "2012-04-03": "清明节",
  "2012-04-04": "清明节",
  "2012-04-29": "劳动节",
  "2012-04-30": "劳动节",
  "2012-05-01": "劳动节",
  "2012-06-22": "端午节",
  "2012-06-23": "端午节",
  "2012-06-24": "端午节",
  "2012-09-30": "中秋节",
  "2012-10-01": "国庆节",
  "2012-10-02": "国庆节",
  "2012-10-03": "国庆节",
  "2012-10-04": "国庆节",
  "2012-10-05": "国庆节",
  "2012-10-06": "国庆节",
  "2012-10-07": "国庆节",
  "2012-10-08": "国庆节",
  "2013-01-01": "元旦",
  "2013-01-02": "元旦",
  "2013-01-03": "元旦",
  "2013-02-09": "春节",
  "2013-02-10": "春节",
  "2013-02-11": "春节",
  "2013-02-12": "春节",
  "2013-02-13": "春节",
  "2013-02-14": "春节",
  "2013-02-15": "春节",
  "2013-04-04": "清明节",
  "2013-04-05": "清明节",
  "2013-04-06": "清明节",
  "2013-04-29": "劳动节",
  "2013-04-30": "劳动节",
  "2013-05-01": "劳动节",
  "2013-06-10": "端午节",
  "2013-06-11": "端午节",
  "2013-06-12": "端午节",
  "2013-09-19": "中秋节",
  "2013-09-20": "中秋节",
  "2013-09-21": "中秋节",
  "2013-10-01": "国庆节",
  "2018-09-29": "补国庆节",
  "2018-09-30": "补国庆节",
  "2018-12-29": "补元旦",
  "2019-02-02": "补春节",
  "2019-02-03": "补春节",
  "2019-04-28": "补劳动节",
  "2019-05-05": "补劳动节",
  "2019-09-29": "补国庆节",
  "2019-10-12": "补国庆节",
  "2020-01-19": "补春节",
  "2020-02-01": "补春节",
  "2020-04-26": "补劳动节",
  "2020-05-09": "补劳动节",
  "2020-06-28": "补端午节",
  "2020-09-27": "补国庆节",
  "2020-10-10": "补国庆节",
  "2021-02-07": "补春节",
  "2021-02-20": "补春节",
  "2021-04-25": "补劳动节",
  "2021-05-08": "补劳动节",
  "2021-09-18": "补中秋节",
  "2021-09-26": "补国庆节",
  "2021-10-09": "补国庆节",
  "2022-01-29": "补春节",
  "2022-01-30": "补春节",
  "2022-04-02": "补清明节",
  "2022-04-24": "补劳动节",
  "2022-05-07": "补劳动节",
  "2022-10-08": "补国庆节",
  "2022-10-09": "补国庆节",
  "2023-01-28": "补春节",
  "2023-01-29": "补春节",
  "2023-04-23": "补劳动节",
  "2023-05-06": "补劳动节",
  "2023-06-25": "补端午节",
  "2023-10-07": "补国庆节",
  "2023-10-08": "补国庆节",
  "2024-02-04": "补春节",
  "2024-02-18": "补春节",
  "2024-04-07": "补清明节",
  "2024-04-28": "补劳动节",
  "2024-05-11": "补劳动节",
  "2024-09-14": "补中秋节",
  "2024-09-29": "补国庆节",
  "2024-10-12": "补国庆节",
  "2025-01-26": "补春节",
  "2025-02-08": "补春节",
  "2025-04-27": "补劳动节",
  "2025-09-28": "补国庆节",
  "2025-10-11": "补国庆节",
  "2026-01-04": "补元旦",
  "2026-02-14": "补春节",
  "2026-02-28": "补春节",
  "2026-05-09": "补劳动节",
  "2026-09-20": "补国庆节",
  "2026-10-10": "补国庆节"
};
function formatDate(day) {
  if (day === void 0) {
    const today = /* @__PURE__ */ new Date();
    const year2 = today.getFullYear();
    const month2 = today.getMonth() + 1;
    const date2 = today.getDate();
    return {
      date: `${year2}-${month2 < 10 ? "0" : ""}${month2}-${date2 < 10 ? "0" : ""}${date2}`,
      weekends: false
      // Assume workday if undefined
    };
  }
  if (day instanceof Date) {
    const year2 = day.getFullYear();
    const month2 = day.getMonth() + 1;
    const date2 = day.getDate();
    const dateStr = `${year2}-${month2 < 10 ? "0" : ""}${month2}-${date2 < 10 ? "0" : ""}${date2}`;
    const dayOfWeek2 = day.getDay();
    return {
      date: dateStr,
      weekends: dayOfWeek2 === 0 || dayOfWeek2 === 6
    };
  }
  if (typeof day === "number") {
    const dateObj = new Date(day);
    const year2 = dateObj.getFullYear();
    const month2 = dateObj.getMonth() + 1;
    const date2 = dateObj.getDate();
    const dateStr = `${year2}-${month2 < 10 ? "0" : ""}${month2}-${date2 < 10 ? "0" : ""}${date2}`;
    const dayOfWeek2 = dateObj.getDay();
    return {
      date: dateStr,
      weekends: dayOfWeek2 === 0 || dayOfWeek2 === 6
    };
  }
  if (day.includes("/")) {
    const parts = day.split("/");
    if (parts.length === 3) {
      const year2 = parseInt(parts[0]);
      const month2 = parseInt(parts[1]) - 1;
      const date2 = parseInt(parts[2]);
      const dateObj = new Date(year2, month2, date2);
      const formattedYear = dateObj.getFullYear();
      const formattedMonth = dateObj.getMonth() + 1;
      const formattedDate = dateObj.getDate();
      const formattedDateStr = `${formattedYear}-${formattedMonth < 10 ? "0" : ""}${formattedMonth}-${formattedDate < 10 ? "0" : ""}${formattedDate}`;
      const dayOfWeek2 = dateObj.getDay();
      return {
        date: formattedDateStr,
        weekends: dayOfWeek2 === 0 || dayOfWeek2 === 6
      };
    }
  }
  const d = new Date(day);
  if (isNaN(d.getTime())) {
    throw new Error(`Invalid date: ${day}`);
  }
  const year = d.getFullYear();
  const month = d.getMonth() + 1;
  const date = d.getDate();
  const dayOfWeek = d.getDay();
  return {
    date: `${year}-${month < 10 ? "0" : ""}${month}-${date < 10 ? "0" : ""}${date}`,
    weekends: dayOfWeek === 0 || dayOfWeek === 6
  };
}
function isWorkday(day) {
  let dateKey;
  if (typeof day === "string" && day.length === 10 && day[4] === "-" && day[7] === "-") {
    dateKey = day;
    const cached2 = dateCache.get(dateKey);
    if (cached2 && cached2.isWorkday !== void 0) {
      return cached2.isWorkday;
    }
    if (WEEKENDS_WORKDAY[dateKey]) {
      dateCache.set(dateKey, { isWorkday: true });
      return true;
    }
    if (HOLIDAYS[dateKey]) {
      dateCache.set(dateKey, { isWorkday: false });
      return false;
    }
    const d = /* @__PURE__ */ new Date(dateKey + "T00:00:00");
    const dayOfWeek = d.getDay();
    const isWeek = dayOfWeek === 0 || dayOfWeek === 6;
    const result2 = !isWeek;
    dateCache.set(dateKey, { isWorkday: result2 });
    return result2;
  }
  const fd = formatDate(day);
  dateKey = fd.date;
  const cached = dateCache.get(dateKey);
  if (cached && cached.isWorkday !== void 0) {
    return cached.isWorkday;
  }
  let result;
  if (WEEKENDS_WORKDAY[dateKey]) {
    result = true;
  } else if (HOLIDAYS[dateKey]) {
    result = false;
  } else {
    result = !fd.weekends;
  }
  dateCache.set(dateKey, { isWorkday: result });
  return result;
}
function isHoliday(day) {
  return !isWorkday(day);
}
function isAdditionalWorkday(day) {
  const fd = formatDate(day);
  return !!WEEKENDS_WORKDAY[fd.date];
}
function isAddtionalWorkday(day) {
  console.warn(
    "DEPRECATED: isAddtionalWorkday is deprecated. Please use isAdditionalWorkday instead."
  );
  return isAdditionalWorkday(day);
}
function getFestival(day) {
  if (typeof day === "string" && day.length === 10 && day[4] === "-" && day[7] === "-") {
    let cached2 = dateCache.get(day);
    if (cached2 && cached2.festival) {
      return cached2.festival;
    }
    let result2 = WEEKENDS_WORKDAY[day];
    if (result2) {
      dateCache.set(day, { festival: result2 });
      return result2;
    }
    const holiday = HOLIDAYS[day];
    if (holiday) {
      dateCache.set(day, { festival: holiday });
      return holiday;
    }
    const dateObj = /* @__PURE__ */ new Date(day + "T00:00:00");
    const dayOfWeek = dateObj.getDay();
    const festival = dayOfWeek === 0 || dayOfWeek === 6 ? "周末" : "工作日";
    dateCache.set(day, { festival });
    return festival;
  }
  const fd = formatDate(day);
  const cached = dateCache.get(fd.date);
  if (cached && cached.festival) {
    return cached.festival;
  }
  let result;
  if (WEEKENDS_WORKDAY[fd.date]) {
    result = WEEKENDS_WORKDAY[fd.date];
  } else if (HOLIDAYS[fd.date]) {
    result = HOLIDAYS[fd.date];
  } else {
    result = fd.weekends ? "周末" : "工作日";
  }
  dateCache.set(fd.date, { festival: result });
  return result;
}
function isWorkdayBatch(days) {
  const dates = days.map((day) => {
    if (typeof day === "string" && day.length === 10 && day[4] === "-" && day[7] === "-") {
      return day;
    }
    return formatDate(day).date;
  });
  return dates.map((date) => {
    const cached = dateCache.get(date);
    if (cached && cached.isWorkday !== void 0) {
      return cached.isWorkday;
    }
    let result;
    if (WEEKENDS_WORKDAY[date]) {
      result = true;
    } else if (HOLIDAYS[date]) {
      result = false;
    } else {
      const d = /* @__PURE__ */ new Date(date + "T00:00:00");
      const dayOfWeek = d.getDay();
      result = !(dayOfWeek === 0 || dayOfWeek === 6);
    }
    dateCache.set(date, { isWorkday: result });
    return result;
  });
}
function isHolidayBatch(days) {
  return days.map((day) => isHoliday(day));
}
function getFestivalBatch(days) {
  const dates = days.map((day) => {
    if (typeof day === "string" && day.length === 10 && day[4] === "-" && day[7] === "-") {
      return day;
    }
    return formatDate(day).date;
  });
  return dates.map((date) => {
    const cached = dateCache.get(date);
    if (cached && cached.festival) {
      return cached.festival;
    }
    let result;
    if (WEEKENDS_WORKDAY[date]) {
      result = WEEKENDS_WORKDAY[date];
    } else if (HOLIDAYS[date]) {
      result = HOLIDAYS[date];
    } else {
      const d = /* @__PURE__ */ new Date(date + "T00:00:00");
      const dayOfWeek = d.getDay();
      result = dayOfWeek === 0 || dayOfWeek === 6 ? "周末" : "工作日";
    }
    dateCache.set(date, { festival: result });
    return result;
  });
}
function getCacheStats() {
  return dateCache.getStats();
}
function clearCache() {
  dateCache.cache.clear();
  dateCache.hits = 0;
  dateCache.misses = 0;
}
function isWeekend(day) {
  const fd = formatDate(day);
  return fd.weekends;
}
function addDays(dayStr, days) {
  const d = /* @__PURE__ */ new Date(dayStr + "T00:00:00");
  d.setDate(d.getDate() + days);
  const year = d.getFullYear();
  const month = String(d.getMonth() + 1).padStart(2, "0");
  const date = String(d.getDate()).padStart(2, "0");
  return `${year}-${month}-${date}`;
}
function nextWorkday(day) {
  let current = formatDate(day).date;
  for (let i = 1; i <= 7; i++) {
    const next = addDays(current, i);
    if (isWorkday(next)) {
      return next;
    }
  }
  return null;
}
function previousWorkday(day) {
  let current = formatDate(day).date;
  for (let i = 1; i <= 14; i++) {
    const prev = addDays(current, -i);
    if (isWorkday(prev)) {
      return prev;
    }
  }
  return null;
}
function countWorkdays(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate > endDate)
    return 0;
  let count = 0;
  let current = startDate;
  while (current <= endDate) {
    if (isWorkday(current))
      count++;
    current = addDays(current, 1);
  }
  return count;
}
function getWorkdaysInRange(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate > endDate)
    return [];
  const result = [];
  let current = startDate;
  while (current <= endDate) {
    if (isWorkday(current))
      result.push(current);
    current = addDays(current, 1);
  }
  return result;
}
function getHolidaysInRange(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate > endDate)
    return [];
  const result = [];
  let current = startDate;
  while (current <= endDate) {
    const festival = getFestival(current);
    if (festival !== "工作日" && festival !== "周末") {
      result.push({ date: current, festival });
    }
    current = addDays(current, 1);
  }
  return result;
}
var LUNAR_NEW_YEAR_DATES = {
  2011: "2011-02-03",
  2012: "2012-01-23",
  2013: "2013-02-10",
  2014: "2014-01-31",
  2015: "2015-02-19",
  2016: "2016-02-08",
  2017: "2017-01-28",
  2018: "2018-02-16",
  2019: "2019-02-05",
  2020: "2020-01-25",
  2021: "2021-02-12",
  2022: "2022-02-01",
  2023: "2023-01-22",
  2024: "2024-02-10",
  2025: "2025-01-29",
  2026: "2026-02-17"
};
var lunarCache = /* @__PURE__ */ new Map();
var LUNAR_MONTH_LENGTHS = [29, 30];
var LUNAR_MONTH_STRINGS = {
  1: "正月",
  2: "二月",
  3: "三月",
  4: "四月",
  5: "五月",
  6: "六月",
  7: "七月",
  8: "八月",
  9: "九月",
  10: "十月",
  11: "冬月",
  12: "腊月"
};
var LUNAR_DAY_STRINGS = {
  1: "初一",
  2: "初二",
  3: "初三",
  4: "初四",
  5: "初五",
  6: "初六",
  7: "初七",
  8: "初八",
  9: "初九",
  10: "初十",
  11: "十一",
  12: "十二",
  13: "十三",
  14: "十四",
  15: "十五",
  16: "十六",
  17: "十七",
  18: "十八",
  19: "十九",
  20: "二十",
  21: "廿一",
  22: "廿二",
  23: "廿三",
  24: "廿四",
  25: "廿五",
  26: "廿六",
  27: "廿七",
  28: "廿八",
  29: "廿九",
  30: "三十"
};
var LUNAR_FESTIVALS = {
  "1-1": "春节",
  "1-15": "元宵节",
  "5-5": "端午节",
  "8-15": "中秋节",
  "9-9": "重阳节",
  "12-8": "腊八节",
  "12-23": "小年"
};
function getLunarInfo(day) {
  const fd = formatDate(day);
  const dateStr = fd.date;
  if (lunarCache.has(dateStr)) {
    return lunarCache.get(dateStr);
  }
  const year = parseInt(dateStr.split("-")[0]);
  let lunarYear = null;
  let daysFromNewYear = 0;
  let lunarNewYearDate = null;
  for (let y = year; y >= year - 1; y--) {
    lunarNewYearDate = LUNAR_NEW_YEAR_DATES[y];
    if (!lunarNewYearDate)
      continue;
    const newYearDate = /* @__PURE__ */ new Date(lunarNewYearDate + "T00:00:00");
    const currentDate = /* @__PURE__ */ new Date(dateStr + "T00:00:00");
    const timeDiff = currentDate.getTime() - newYearDate.getTime();
    daysFromNewYear = Math.floor(timeDiff / (1e3 * 60 * 60 * 24));
    if (daysFromNewYear >= 0) {
      lunarYear = y;
      break;
    }
  }
  if (lunarYear === null) {
    const result2 = {
      date: dateStr,
      lunarYear: null,
      lunarMonth: null,
      lunarDay: null,
      lunarString: "",
      lunarFestival: "",
      dayOfWeek: fd.weekends ? "周末" : "工作日"
    };
    lunarCache.set(dateStr, result2);
    return result2;
  }
  let remainingDays = daysFromNewYear;
  let lunarMonth = 1;
  let lunarDay = 1;
  while (remainingDays >= LUNAR_MONTH_LENGTHS[(lunarMonth - 1) % 2]) {
    remainingDays -= LUNAR_MONTH_LENGTHS[(lunarMonth - 1) % 2];
    lunarMonth++;
    if (lunarMonth > 12) {
      lunarMonth = 1;
      lunarYear++;
    }
  }
  lunarDay = remainingDays + 1;
  const lunarMonthStr = LUNAR_MONTH_STRINGS[lunarMonth] || `${lunarMonth}月`;
  const lunarDayStr = LUNAR_DAY_STRINGS[lunarDay] || `${lunarDay}`;
  const lunarString = lunarMonthStr + lunarDayStr;
  const lunarFestival = LUNAR_FESTIVALS[`${lunarMonth}-${lunarDay}`] || "";
  const result = {
    date: dateStr,
    lunarYear,
    lunarMonth,
    lunarDay,
    lunarString,
    lunarFestival,
    dayOfWeek: fd.weekends ? "周末" : "工作日"
  };
  lunarCache.set(dateStr, result);
  return result;
}
function getWorkdaysInterval(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate === endDate)
    return 0;
  const startObj = /* @__PURE__ */ new Date(startDate + "T00:00:00");
  const endObj = /* @__PURE__ */ new Date(endDate + "T00:00:00");
  if (startObj > endObj) {
    let count = 0;
    let current = previousWorkday(startDate);
    while (current && current >= endDate) {
      count++;
      current = previousWorkday(current);
    }
    return count;
  } else {
    let count = 0;
    let current = nextWorkday(startDate);
    while (current && current <= endDate) {
      count++;
      current = nextWorkday(current);
    }
    return count;
  }
}
function addWorkdays(day, n) {
  const startDate = formatDate(day).date;
  if (n === 0) {
    return isWorkday(startDate) ? startDate : nextWorkday(startDate);
  }
  let current = startDate;
  let remaining = Math.abs(n);
  if (n > 0) {
    while (remaining > 0) {
      current = nextWorkday(current);
      if (!current)
        return null;
      remaining--;
    }
  } else {
    while (remaining > 0) {
      current = previousWorkday(current);
      if (!current)
        return null;
      remaining--;
    }
  }
  return current;
}
function getWorkdaySequence(start, end, interval = 1) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate > endDate)
    return [];
  const result = [];
  let current = isWorkday(startDate) ? startDate : nextWorkday(startDate);
  while (current && current <= endDate) {
    result.push(current);
    current = addWorkdays(current, interval);
    if (!current || current > endDate)
      break;
  }
  return result;
}
function getAnnualStats(year) {
  if (typeof year !== "number" || year < 2011 || year > 2026) {
    throw new Error("Year must be between 2011 and 2026");
  }
  const yearStr = String(year);
  const startDate = `${yearStr}-01-01`;
  const endDate = `${yearStr}-12-31`;
  let totalDays = 0;
  let workdays = 0;
  let holidays = 0;
  let weekends = 0;
  let additionalWorkdays = 0;
  let festivalCounts = {};
  let current = startDate;
  while (current <= endDate) {
    totalDays++;
    if (isWorkday(current)) {
      workdays++;
      if (isAddtionalWorkday(current)) {
        additionalWorkdays++;
      }
    } else {
      if (HOLIDAYS[current]) {
        holidays++;
        const festival = HOLIDAYS[current];
        festivalCounts[festival] = (festivalCounts[festival] || 0) + 1;
      } else {
        weekends++;
      }
    }
    const dateObj = /* @__PURE__ */ new Date(current + "T00:00:00");
    dateObj.setDate(dateObj.getDate() + 1);
    const nextYear = dateObj.getFullYear();
    const nextMonth = String(dateObj.getMonth() + 1).padStart(2, "0");
    const nextDate = String(dateObj.getDate()).padStart(2, "0");
    current = `${nextYear}-${nextMonth}-${nextDate}`;
  }
  return {
    year,
    totalDays,
    workdays,
    holidays,
    weekends,
    additionalWorkdays,
    workdayPercentage: parseFloat((workdays / totalDays * 100).toFixed(2)),
    holidayDistribution: festivalCounts
  };
}
function getNextHoliday(day) {
  const startDate = formatDate(day).date;
  let current = startDate;
  const dateObj = /* @__PURE__ */ new Date(current + "T00:00:00");
  for (let i = 0; i < 365; i++) {
    dateObj.setDate(dateObj.getDate() + 1);
    const year = dateObj.getFullYear();
    const month = String(dateObj.getMonth() + 1).padStart(2, "0");
    const date = String(dateObj.getDate()).padStart(2, "0");
    current = `${year}-${month}-${date}`;
    if (HOLIDAYS[current]) {
      return {
        date: current,
        festival: HOLIDAYS[current],
        daysUntil: i + 1
      };
    }
  }
  return null;
}
function daysUntilHoliday(day) {
  const nextHoliday = getNextHoliday(day);
  return nextHoliday ? nextHoliday.daysUntil : -1;
}
function isHolidayApproaching(day, daysBefore = 1) {
  const startDate = formatDate(day).date;
  const dateObj = /* @__PURE__ */ new Date(startDate + "T00:00:00");
  for (let i = 1; i <= daysBefore; i++) {
    dateObj.setDate(dateObj.getDate() + 1);
    const year = dateObj.getFullYear();
    const month = String(dateObj.getMonth() + 1).padStart(2, "0");
    const date = String(dateObj.getDate()).padStart(2, "0");
    const futureDate = `${year}-${month}-${date}`;
    if (HOLIDAYS[futureDate]) {
      return true;
    }
  }
  return false;
}
function getConsecutiveHolidays(day) {
  const startDate = formatDate(day).date;
  if (!HOLIDAYS[startDate]) {
    return 0;
  }
  let count = 1;
  let current = startDate;
  const dateObj = /* @__PURE__ */ new Date(current + "T00:00:00");
  while (true) {
    dateObj.setDate(dateObj.getDate() + 1);
    const year = dateObj.getFullYear();
    const month = String(dateObj.getMonth() + 1).padStart(2, "0");
    const date = String(dateObj.getDate()).padStart(2, "0");
    current = `${year}-${month}-${date}`;
    if (HOLIDAYS[current] || !isWorkday(current) && !isAddtionalWorkday(current)) {
      count++;
    } else {
      break;
    }
  }
  return count;
}
var customSchedules = {};
function setWorkSchedule(scheduleId, schedule) {
  const defaultSchedule = {
    workdays: [1, 2, 3, 4, 5],
    // Monday to Friday
    holidays: [],
    workdaysOnWeekends: []
  };
  customSchedules[scheduleId] = { ...defaultSchedule, ...schedule };
}
function getWorkSchedule(scheduleId) {
  return customSchedules[scheduleId] || null;
}
function isWorkdayCustom(day, scheduleId = "default") {
  const schedule = customSchedules[scheduleId];
  if (!schedule) {
    return isWorkday(day);
  }
  const fd = formatDate(day);
  const dateStr = fd.date;
  const cacheKey = `custom_${scheduleId}_${dateStr}`;
  const cached = dateCache.get(cacheKey);
  if (cached && cached.isWorkday !== void 0) {
    return cached.isWorkday;
  }
  if (schedule.workdaysOnWeekends.includes(dateStr)) {
    dateCache.set(cacheKey, { isWorkday: true });
    return true;
  }
  if (schedule.holidays.includes(dateStr)) {
    dateCache.set(cacheKey, { isWorkday: false });
    return false;
  }
  const dayOfWeek = (/* @__PURE__ */ new Date(dateStr + "T00:00:00")).getDay();
  const isCustomWorkday = schedule.workdays.includes(dayOfWeek);
  const result = isCustomWorkday;
  dateCache.set(cacheKey, { isWorkday: result });
  return result;
}
function isHolidayCustom(day, scheduleId = "default") {
  return !isWorkdayCustom(day, scheduleId);
}
function clearWorkSchedule(scheduleId) {
  delete customSchedules[scheduleId];
}
function getAvailableSchedules() {
  return Object.keys(customSchedules);
}
function getMonthlyStats(year, month) {
  if (typeof year !== "number" || year < 2011 || year > 2026) {
    throw new Error("Year must be between 2011 and 2026");
  }
  if (typeof month !== "number" || month < 1 || month > 12) {
    throw new Error("Month must be between 1 and 12");
  }
  const monthStr = String(year) + "-" + String(month).padStart(2, "0");
  const startDate = monthStr + "-01";
  const dateObj = new Date(year, month, 0);
  const daysInMonth = dateObj.getDate();
  const endDate = monthStr + "-" + String(daysInMonth).padStart(2, "0");
  let totalDays = 0;
  let workdays = 0;
  let holidays = 0;
  let weekends = 0;
  let additionalWorkdays = 0;
  let current = startDate;
  while (current <= endDate) {
    totalDays++;
    if (isWorkday(current)) {
      workdays++;
      if (isAddtionalWorkday(current)) {
        additionalWorkdays++;
      }
    } else {
      if (HOLIDAYS[current]) {
        holidays++;
      } else {
        weekends++;
      }
    }
    const dateObj2 = /* @__PURE__ */ new Date(current + "T00:00:00");
    dateObj2.setDate(dateObj2.getDate() + 1);
    const nextYear = dateObj2.getFullYear();
    const nextMonth = String(dateObj2.getMonth() + 1).padStart(2, "0");
    const nextDate = String(dateObj2.getDate()).padStart(2, "0");
    current = `${nextYear}-${nextMonth}-${nextDate}`;
  }
  return {
    year,
    month,
    totalDays,
    workdays,
    holidays,
    weekends,
    additionalWorkdays,
    workdayPercentage: parseFloat((workdays / totalDays * 100).toFixed(2))
  };
}
function getWorkdayRatio(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  if (startDate > endDate) {
    throw new Error("Start date must be before end date");
  }
  let totalDays = 0;
  let workdays = 0;
  let holidays = 0;
  let weekends = 0;
  let additionalWorkdays = 0;
  let current = startDate;
  while (current <= endDate) {
    totalDays++;
    if (isWorkday(current)) {
      workdays++;
      if (isAddtionalWorkday(current)) {
        additionalWorkdays++;
      }
    } else {
      if (HOLIDAYS[current]) {
        holidays++;
      } else {
        weekends++;
      }
    }
    const dateObj = /* @__PURE__ */ new Date(current + "T00:00:00");
    dateObj.setDate(dateObj.getDate() + 1);
    const nextYear = dateObj.getFullYear();
    const nextMonth = String(dateObj.getMonth() + 1).padStart(2, "0");
    const nextDate = String(dateObj.getDate()).padStart(2, "0");
    current = `${nextYear}-${nextMonth}-${nextDate}`;
  }
  return {
    startDate,
    endDate,
    totalDays,
    workdays,
    holidays,
    weekends,
    additionalWorkdays,
    workdayCount: workdays,
    workdayPercentage: parseFloat((workdays / totalDays * 100).toFixed(2)),
    holidayPercentage: parseFloat((holidays / totalDays * 100).toFixed(2)),
    weekendPercentage: parseFloat((weekends / totalDays * 100).toFixed(2))
  };
}
function getMostCommonHoliday(year) {
  const stats = getAnnualStats(year);
  if (!stats || !stats.holidayDistribution) {
    return null;
  }
  let mostCommon = null;
  let maxCount = 0;
  for (const [festival, count] of Object.entries(stats.holidayDistribution)) {
    if (count > maxCount) {
      maxCount = count;
      mostCommon = { festival, count };
    }
  }
  return mostCommon;
}
function getHolidaysByFestival(start, end) {
  const startFormatted = formatDate(start).date;
  const endFormatted = formatDate(end).date;
  if (startFormatted > endFormatted) {
    throw new Error("Start date must be before end date");
  }
  const festivalMap = {};
  let current = startFormatted;
  while (current <= endFormatted) {
    const festival = HOLIDAYS[current];
    if (festival) {
      if (!festivalMap[festival]) {
        festivalMap[festival] = [];
      }
      festivalMap[festival].push(current);
    }
    const dateObj = /* @__PURE__ */ new Date(current + "T00:00:00");
    dateObj.setDate(dateObj.getDate() + 1);
    const nextYear = dateObj.getFullYear();
    const nextMonth = String(dateObj.getMonth() + 1).padStart(2, "0");
    const nextDate = String(dateObj.getDate()).padStart(2, "0");
    current = `${nextYear}-${nextMonth}-${nextDate}`;
  }
  return festivalMap;
}
function getTotalDays(start, end) {
  const startDate = formatDate(start).date;
  const endDate = formatDate(end).date;
  const startObj = /* @__PURE__ */ new Date(startDate + "T00:00:00");
  const endObj = /* @__PURE__ */ new Date(endDate + "T00:00:00");
  const timeDiff = endObj.getTime() - startObj.getTime();
  return Math.abs(Math.floor(timeDiff / (1e3 * 60 * 60 * 24))) + 1;
}
function calculateWorkHours(start, end, hoursPerDay = 8) {
  const workdays = countWorkdays(start, end);
  return workdays * hoursPerDay;
}
function getWeekRange(day, startDay = 1) {
  const fd = formatDate(day);
  const dateObj = /* @__PURE__ */ new Date(fd.date + "T00:00:00");
  const currentDayOfWeek = dateObj.getDay();
  const daysToSubtract = (currentDayOfWeek - startDay + 7) % 7;
  const startOfWeek = new Date(dateObj);
  startOfWeek.setDate(dateObj.getDate() - daysToSubtract);
  const endOfWeek = new Date(startOfWeek);
  endOfWeek.setDate(startOfWeek.getDate() + 6);
  const startYear = startOfWeek.getFullYear();
  const startMonth = String(startOfWeek.getMonth() + 1).padStart(2, "0");
  const startDate = String(startOfWeek.getDate()).padStart(2, "0");
  const weekStart = `${startYear}-${startMonth}-${startDate}`;
  const endYear = endOfWeek.getFullYear();
  const endMonth = String(endOfWeek.getMonth() + 1).padStart(2, "0");
  const endDate = String(endOfWeek.getDate()).padStart(2, "0");
  const weekEnd = `${endYear}-${endMonth}-${endDate}`;
  return {
    startDate: weekStart,
    endDate: weekEnd
  };
}
function getMonthRange(day) {
  const fd = formatDate(day);
  const parts = fd.date.split("-");
  const year = parseInt(parts[0]);
  const month = parseInt(parts[1]) - 1;
  const startOfMonth = new Date(year, month, 1);
  const endOfMonth = new Date(year, month + 1, 0);
  const startYear = startOfMonth.getFullYear();
  const startMonth = String(startOfMonth.getMonth() + 1).padStart(2, "0");
  const startDate = String(startOfMonth.getDate()).padStart(2, "0");
  const monthStart = `${startYear}-${startMonth}-${startDate}`;
  const endYear = endOfMonth.getFullYear();
  const endMonth = String(endOfMonth.getMonth() + 1).padStart(2, "0");
  const endDate = String(endOfMonth.getDate()).padStart(2, "0");
  const monthEnd = `${endYear}-${endMonth}-${endDate}`;
  return {
    startDate: monthStart,
    endDate: monthEnd
  };
}
function isWithinOfficeHours(date, options = {}) {
  if (!(date instanceof Date)) {
    date = new Date(date);
  }
  const {
    startHour = 9,
    endHour = 18,
    startDay = 1,
    // Monday
    endDay = 5
    // Friday
  } = options;
  const dayOfWeek = date.getDay();
  const hour = date.getHours();
  if (dayOfWeek < startDay || dayOfWeek > endDay) {
    return false;
  }
  const dateStr = formatDate(date).date;
  if (!isWorkday(dateStr)) {
    return false;
  }
  if (hour < startHour || hour >= endHour) {
    return false;
  }
  return true;
}
var leaveBalances = {};
function setLeaveBalance(userId, balances) {
  const defaultBalances = {
    annual: 0,
    sick: 0,
    personal: 0,
    marriage: 0,
    maternity: 0,
    paternity: 0,
    custom: {}
  };
  leaveBalances[userId] = { ...defaultBalances, ...balances };
}
function getLeaveBalance(userId) {
  return leaveBalances[userId] || null;
}
function applyLeave(userId, leaveType, startDate, endDate, includeWorkdays = true) {
  const userBalance = leaveBalances[userId];
  if (!userBalance) {
    return { success: false, message: "User not found", remainingBalance: null };
  }
  let leaveDays;
  if (includeWorkdays) {
    leaveDays = countWorkdays(startDate, endDate);
  } else {
    leaveDays = getTotalDays(startDate, endDate);
  }
  if (leaveType === "custom") {
    return {
      success: false,
      message: "For custom leave, specify the exact custom type",
      remainingBalance: userBalance
    };
  } else if (userBalance.hasOwnProperty(leaveType)) {
    if (userBalance[leaveType] < leaveDays) {
      return {
        success: false,
        message: `Insufficient ${leaveType} leave balance`,
        remainingBalance: userBalance
      };
    }
    userBalance[leaveType] -= leaveDays;
    return {
      success: true,
      message: `${leaveDays} days of ${leaveType} leave applied successfully`,
      leaveDays,
      remainingBalance: { ...userBalance }
    };
  } else {
    return {
      success: false,
      message: `Unknown leave type: ${leaveType}`,
      remainingBalance: userBalance
    };
  }
}
function addLeaveDays(userId, leaveType, days) {
  const userBalance = leaveBalances[userId];
  if (!userBalance) {
    return { success: false, message: "User not found", remainingBalance: null };
  }
  if (userBalance.hasOwnProperty(leaveType)) {
    userBalance[leaveType] = (userBalance[leaveType] || 0) + days;
    return {
      success: true,
      message: `${days} days added to ${leaveType} leave`,
      remainingBalance: { ...userBalance }
    };
  } else if (leaveType === "custom" && typeof days === "object") {
    for (const [customType, customDays] of Object.entries(days)) {
      userBalance.custom[customType] = (userBalance.custom[customType] || 0) + customDays;
    }
    return {
      success: true,
      message: "Custom leave days added",
      remainingBalance: { ...userBalance }
    };
  } else if (typeof days === "number" && leaveType.startsWith("custom_")) {
    userBalance.custom[leaveType.substring(7)] = (userBalance.custom[leaveType.substring(7)] || 0) + days;
    return {
      success: true,
      message: `${days} days added to custom ${leaveType.substring(7)} leave`,
      remainingBalance: { ...userBalance }
    };
  } else {
    return {
      success: false,
      message: `Unknown leave type: ${leaveType}`,
      remainingBalance: userBalance
    };
  }
}
function calculateActualWorkdays(startDate, endDate, leaveRecords = []) {
  const totalWorkdays = countWorkdays(startDate, endDate);
  let totalLeaveWorkdays = 0;
  for (const leave of leaveRecords) {
    if (leave.approved) {
      const leaveStart = formatDate(leave.startDate).date;
      const leaveEnd = formatDate(leave.endDate).date;
      const rangeStart = formatDate(startDate).date;
      const rangeEnd = formatDate(endDate).date;
      const intersectStart = leaveStart > rangeStart ? leaveStart : rangeStart;
      const intersectEnd = leaveEnd < rangeEnd ? leaveEnd : rangeEnd;
      if (intersectStart <= intersectEnd) {
        totalLeaveWorkdays += countWorkdays(intersectStart, intersectEnd);
      }
    }
  }
  return Math.max(0, totalWorkdays - totalLeaveWorkdays);
}
function generateCalendar(year, month, options = {}) {
  const {
    startDay = 1,
    // Monday
    includeFestival = true,
    includeLunar = false
  } = options;
  const firstDay = new Date(year, month - 1, 1);
  const lastDay = new Date(year, month, 0);
  const daysInMonth = lastDay.getDate();
  const monthStartDay = firstDay.getDay();
  const prevMonthOffset = (monthStartDay - startDay + 7) % 7;
  const calendar = [];
  let week = [];
  const prevMonthLastDay = new Date(year, month - 1, 0).getDate();
  for (let i = 0; i < prevMonthOffset; i++) {
    const dayNum = prevMonthLastDay - prevMonthOffset + i + 1;
    const prevMonth = month === 1 ? 12 : month - 1;
    const prevYear = month === 1 ? year - 1 : year;
    const dateStr = `${prevYear}-${String(prevMonth).padStart(2, "0")}-${String(dayNum).padStart(2, "0")}`;
    const dayInfo = {
      year: prevYear,
      month: prevMonth,
      date: dayNum,
      dayType: "prevMonth",
      isWorkday: isWorkday(dateStr),
      dateStr
    };
    if (includeFestival) {
      dayInfo.festival = getFestival(dateStr);
    }
    if (includeLunar) {
      const lunarInfo = getLunarInfo(dateStr);
      dayInfo.lunar = lunarInfo.lunarString;
    }
    week.push(dayInfo);
  }
  for (let day = 1; day <= daysInMonth; day++) {
    const dateStr = `${year}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
    const dayInfo = {
      year,
      month,
      date: day,
      dayType: "currentMonth",
      isWorkday: isWorkday(dateStr),
      dateStr
    };
    if (includeFestival) {
      dayInfo.festival = getFestival(dateStr);
    }
    if (includeLunar) {
      const lunarInfo = getLunarInfo(dateStr);
      dayInfo.lunar = lunarInfo.lunarString;
    }
    week.push(dayInfo);
    if (week.length === 7) {
      calendar.push(week);
      week = [];
    }
  }
  const remainingDays = 7 - week.length;
  for (let i = 0; i < remainingDays; i++) {
    const dayNum = i + 1;
    const nextMonth = month === 12 ? 1 : month + 1;
    const nextYear = month === 12 ? year + 1 : year;
    const dateStr = `${nextYear}-${String(nextMonth).padStart(2, "0")}-${String(dayNum).padStart(2, "0")}`;
    const dayInfo = {
      year: nextYear,
      month: nextMonth,
      date: dayNum,
      dayType: "nextMonth",
      isWorkday: isWorkday(dateStr),
      dateStr
    };
    if (includeFestival) {
      dayInfo.festival = getFestival(dateStr);
    }
    if (includeLunar) {
      const lunarInfo = getLunarInfo(dateStr);
      dayInfo.lunar = lunarInfo.lunarString;
    }
    week.push(dayInfo);
  }
  if (week.length > 0) {
    calendar.push(week);
  }
  return calendar;
}
function generateCompactCalendar(year, month) {
  const calendar = generateCalendar(year, month);
  let workdays = 0;
  let holidays = 0;
  let weekends = 0;
  for (const week of calendar) {
    for (const day of week) {
      if (day.dayType === "currentMonth") {
        if (day.isWorkday) {
          workdays++;
        } else {
          if (HOLIDAYS[day.dateStr]) {
            holidays++;
          } else {
            weekends++;
          }
        }
      }
    }
  }
  return {
    year,
    month,
    calendar,
    stats: {
      workdays,
      holidays,
      weekends,
      total: workdays + holidays + weekends
    }
  };
}
function getDaysInMonth(year, month, type = "workdays") {
  const result = [];
  const firstDay = new Date(year, month - 1, 1);
  const lastDay = new Date(year, month, 0);
  const daysInMonth = lastDay.getDate();
  for (let day = 1; day <= daysInMonth; day++) {
    const dateStr = `${year}-${String(month).padStart(2, "0")}-${String(day).padStart(2, "0")}`;
    let include = false;
    if (type === "workdays") {
      include = isWorkday(dateStr);
    } else if (type === "holidays") {
      include = !isWorkday(dateStr) && HOLIDAYS[dateStr];
    } else if (type === "weekends") {
      include = !isWorkday(dateStr) && !HOLIDAYS[dateStr];
    } else {
      include = true;
    }
    if (include) {
      result.push(dateStr);
    }
  }
  return result;
}
function isWorkdayInTimezone(date, timeZone = "Asia/Shanghai") {
  let dateObj;
  if (typeof date === "string") {
    if (/^\d{4}-\d{2}-\d{2}$/.test(date)) {
      const [year2, month2, day2] = date.split("-").map(Number);
      dateObj = new Date(Date.UTC(year2, month2 - 1, day2));
      const tzDate = new Date(dateObj.toLocaleString("en-US", { timeZone }));
      const tzYear = tzDate.getFullYear();
      const tzMonth = tzDate.getMonth() + 1;
      const tzDay = tzDate.getDate();
      const dateStr2 = `${tzYear}-${String(tzMonth).padStart(2, "0")}-${String(tzDay).padStart(2, "0")}`;
      return isWorkday(dateStr2);
    } else {
      dateObj = new Date(date);
    }
  } else if (date instanceof Date) {
    dateObj = date;
  } else if (typeof date === "number") {
    dateObj = new Date(date);
  } else {
    throw new Error("Invalid date format");
  }
  const options = {
    timeZone,
    year: "numeric",
    month: "2-digit",
    day: "2-digit"
  };
  const formatter = new Intl.DateTimeFormat("en-CA", options);
  const [year, month, day] = formatter.format(dateObj).split("-");
  const dateStr = `${year}-${month}-${day}`;
  return isWorkday(dateStr);
}
function getWorkdayInMultipleTimezones(timestamp, timezones) {
  const results = {};
  for (const tz of timezones) {
    results[tz] = isWorkdayInTimezone(timestamp, tz);
  }
  return results;
}
function convertTimezoneAndCheckWorkday(date, fromTimeZone, toTimeZone, checkWorkday = true) {
  let dateObj;
  if (typeof date === "string") {
    if (/^\d{4}-\d{2}-\d{2}$/.test(date)) {
      const [year, month, day] = date.split("-").map(Number);
      dateObj = new Date(year, month - 1, day, 12, 0, 0);
    } else {
      dateObj = new Date(date);
    }
  } else if (date instanceof Date) {
    dateObj = date;
  } else if (typeof date === "number") {
    dateObj = new Date(date);
  } else {
    throw new Error("Invalid date format");
  }
  const sourceTimeOptions = {
    timeZone: fromTimeZone,
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
    second: "2-digit",
    hour12: false
  };
  const sourceFormatter = new Intl.DateTimeFormat("en-CA", sourceTimeOptions);
  const sourceFormatted = sourceFormatter.formatToParts(dateObj);
  let sourceYear, sourceMonth, sourceDay, sourceHour, sourceMinute, sourceSecond;
  for (const part of sourceFormatted) {
    switch (part.type) {
      case "year":
        sourceYear = part.value;
        break;
      case "month":
        sourceMonth = part.value;
        break;
      case "day":
        sourceDay = part.value;
        break;
      case "hour":
        sourceHour = part.value;
        break;
      case "minute":
        sourceMinute = part.value;
        break;
      case "second":
        sourceSecond = part.value;
        break;
    }
  }
  const sourceDate = /* @__PURE__ */ new Date(
    `${sourceYear}-${sourceMonth}-${sourceDay}T${sourceHour}:${sourceMinute}:${sourceSecond}`
  );
  const targetDate = new Date(sourceDate.toLocaleString("en-US", { timeZone: toTimeZone }));
  const targetYear = targetDate.getFullYear();
  const targetMonth = targetDate.getMonth() + 1;
  const targetDay = targetDate.getDate();
  const targetDateStr = `${targetYear}-${String(targetMonth).padStart(2, "0")}-${String(targetDay).padStart(2, "0")}`;
  return {
    originalDate: formatDate(date).date,
    convertedDate: targetDateStr,
    isWorkdayInChina: checkWorkday ? isWorkday(targetDateStr) : void 0,
    fromTimezone: fromTimeZone,
    toTimezone: toTimeZone
  };
}
var scheduledEvents = {};
var eventTimers = {};
function scheduleEventNextWorkday(eventId, callback, startDate) {
  if (typeof callback !== "function") {
    throw new Error("Callback must be a function");
  }
  const start = startDate ? formatDate(startDate).date : formatDate(/* @__PURE__ */ new Date()).date;
  const nextWorkdayDate = nextWorkday(start);
  if (!nextWorkdayDate) {
    throw new Error("Could not find next workday within reasonable range");
  }
  const eventDate = /* @__PURE__ */ new Date(nextWorkdayDate + "T00:00:00");
  const now = /* @__PURE__ */ new Date();
  const timeUntilEvent = eventDate.getTime() - now.getTime();
  if (eventTimers[eventId]) {
    clearTimeout(eventTimers[eventId]);
  }
  scheduledEvents[eventId] = {
    callback,
    scheduledDate: nextWorkdayDate,
    type: "next_workday"
  };
  if (timeUntilEvent > 0) {
    eventTimers[eventId] = setTimeout(() => {
      callback(nextWorkdayDate);
      delete scheduledEvents[eventId];
      delete eventTimers[eventId];
    }, timeUntilEvent);
  } else {
    callback(nextWorkdayDate);
    delete scheduledEvents[eventId];
    delete eventTimers[eventId];
  }
  return eventId;
}
function scheduleEventOnWorkday(eventId, callback, targetDate) {
  if (typeof callback !== "function") {
    throw new Error("Callback must be a function");
  }
  const target = formatDate(targetDate).date;
  if (!isWorkday(target)) {
    throw new Error(`Target date ${target} is not a workday`);
  }
  const eventDate = /* @__PURE__ */ new Date(target + "T00:00:00");
  const now = /* @__PURE__ */ new Date();
  const timeUntilEvent = eventDate.getTime() - now.getTime();
  if (eventTimers[eventId]) {
    clearTimeout(eventTimers[eventId]);
  }
  scheduledEvents[eventId] = {
    callback,
    scheduledDate: target,
    type: "specific_workday"
  };
  if (timeUntilEvent > 0) {
    eventTimers[eventId] = setTimeout(() => {
      callback(target);
      delete scheduledEvents[eventId];
      delete eventTimers[eventId];
    }, timeUntilEvent);
  } else {
    callback(target);
    delete scheduledEvents[eventId];
    delete eventTimers[eventId];
  }
  return eventId;
}
function scheduleEventAfterWorkdays(eventId, callback, workdays, startDate) {
  if (typeof callback !== "function") {
    throw new Error("Callback must be a function");
  }
  if (typeof workdays !== "number" || workdays < 1) {
    throw new Error("Workdays must be a positive number");
  }
  const start = startDate ? formatDate(startDate).date : formatDate(/* @__PURE__ */ new Date()).date;
  const futureDate = addWorkdays(start, workdays);
  if (!futureDate) {
    throw new Error(`Could not calculate a date ${workdays} workdays from ${start}`);
  }
  return scheduleEventOnWorkday(eventId, callback, futureDate);
}
function cancelScheduledEvent(eventId) {
  if (eventTimers[eventId]) {
    clearTimeout(eventTimers[eventId]);
    delete eventTimers[eventId];
    delete scheduledEvents[eventId];
    return true;
  }
  return false;
}
function getScheduledEvent(eventId) {
  return scheduledEvents[eventId] || null;
}
function getAllScheduledEvents() {
  const events = [];
  for (const [id, event] of Object.entries(scheduledEvents)) {
    events.push({
      eventId: id,
      ...event
    });
  }
  return events;
}
export {
  addLeaveDays,
  addWorkdays,
  applyLeave,
  calculateActualWorkdays,
  calculateWorkHours,
  cancelScheduledEvent,
  clearCache,
  clearWorkSchedule,
  convertTimezoneAndCheckWorkday,
  countWorkdays,
  daysUntilHoliday,
  generateCalendar,
  generateCompactCalendar,
  getAllScheduledEvents,
  getAnnualStats,
  getAvailableSchedules,
  getCacheStats,
  getConsecutiveHolidays,
  getDaysInMonth,
  getFestival,
  getFestivalBatch,
  getHolidaysByFestival,
  getHolidaysInRange,
  getLeaveBalance,
  getLunarInfo,
  getMonthRange,
  getMonthlyStats,
  getMostCommonHoliday,
  getNextHoliday,
  getScheduledEvent,
  getTotalDays,
  getWeekRange,
  getWorkSchedule,
  getWorkdayInMultipleTimezones,
  getWorkdayRatio,
  getWorkdaySequence,
  getWorkdaysInRange,
  getWorkdaysInterval,
  isAdditionalWorkday,
  isAddtionalWorkday,
  isHoliday,
  isHolidayApproaching,
  isHolidayBatch,
  isHolidayCustom,
  isWeekend,
  isWithinOfficeHours,
  isWorkday,
  isWorkdayBatch,
  isWorkdayCustom,
  isWorkdayInTimezone,
  nextWorkday,
  previousWorkday,
  scheduleEventAfterWorkdays,
  scheduleEventNextWorkday,
  scheduleEventOnWorkday,
  setLeaveBalance,
  setWorkSchedule
};
//# sourceMappingURL=chinese-workday.js.map
