# Management-System AI 技术分享

> 面向团队内部的技术分享文档，聚焦于"用了什么技术、解决了什么问题"。

---

## 一、AI 功能模块总览

系统目前共有 **5 个 AI 功能模块**：

| # | 模块 | 做什么 | 入口 |
|:---:|------|-------|------|
| 1 | **知识库** | 自动同步钉钉知识库文档到本地，提供文档浏览、混合检索（全文+语义）和 RAG 聊天问答 | Web 前端 |
| 2 | **任务多维度分析** | 拉取用户 TB 任务数据 → LLM 提取关键词 → 知识库检索 → LLM 生成 4 模块分析建议报告 | Web 前端 |
| 3 | **AI 创建 TB 任务** | AI 引导式问答收集任务信息，自动生成建议产出和需求描述，写入草稿后与前端面板双向同步，确认后调钉钉 API 创建正式任务单 | Web 前端 + ttyd 终端 |
| 4 | **AI 交互终端** | 浏览器内嵌 ttyd 网页终端，在隔离环境中运行 Claude CLI，提供完整的 AI 对话和操作能力 | Web 前端（iframe） |
| 5 | **MCP 服务** | 独立进程，通过 MCP 协议给 Claude Code（桌面端）提供 Tools、Resources、Prompts，让开发者在终端里直接调用业务能力 | Claude Code（桌面） |

```
                     用  户  入  口
        ┌─────────────────┼─────────────────┐
        ↓                 ↓                  ↓
    Web 前端          ttyd 终端          Claude Code
  (模块 1/2/3/4)     (模块 3/4)          (模块 5)
        │                 │                  │
        └────────┬────────┘                  │
            HTTP/SSE                    MCP/SSE
                 │                          │
                 ↓                          ↓
        ┌────────────────────────────────────────┐
        │            AI 共 享 能 力 层            │
        ├────────────────────────────────────────┤
        │ 混合检索 · RAG · LLM 调用 · SSE 流式    │
        │ Skills 规则引擎 · MCP 协议 · 会话管理   │
        └────────────────────────────────────────┘
                            ↓
        ┌────────────────────────────────────────┐
        │         数 据 & 外 部 依 赖             │
        ├────────────────────────────────────────┤
        │ MySQL · pgvector · one-api 网关         │
        │ SentenceTransformers · 钉钉 API         │
        └────────────────────────────────────────┘
```

---

## 二、知识库 & RAG（检索增强生成）

这是所有 AI 功能的基础——让 LLM 能"读懂"公司内部文档。没有这一步，后面的任务分析、智能问答都是空谈。

### 2.1 整体数据流

```
钉钉知识库 API
    ↓ 自动同步（每天凌晨 4:00）
文档解析 → 文档分类 → 智能分块 → 向量嵌入
    ↓                              ↓
MySQL (kb_documents + kb_chunks)   pgvector (chunk_vectors)
    ↓                              ↓
FULLTEXT 关键词搜索              cosine 语义搜索
    ↓______________________________↓
              RRF 融合排序
                 ↓
            最终检索结果
```

### 2.2 文档同步：从钉钉到本地

通过钉钉知识库 API 自动拉取企业内部文档：

- **发现阶段**：列出所有已启用的知识库 workspace，支持通过配置文件按名称/优先级管理。目前对接了 10 个知识库空间（本体开发部、研发共享文档、研发体系文档、产品专项知识库等）
- **遍历阶段**：递归遍历每个 workspace 的节点树，支持阿里文档、在线表格（Workbook）、普通文件三种类型
- **增量同步**：对比本地已有文档版本，只拉取变更内容
- **定时全量刷新**：每天凌晨 4:00 自动执行同步 + 重新分块 + 重新嵌入

### 2.3 文档分块：为什么不能直接塞给 LLM

长文档直接检索有两个问题：检索精度差（一篇 50 页的规范文档，搜到后 LLM 不知道重点在哪）、token 浪费（无关部分也占上下文窗口）。

系统根据文档类型**自动选择 4 种分块策略**：

| 策略 | 适用场景 | 思路 | 示例 |
|------|---------|------|------|
| **结构化** | 技术文档、规范 | 按 `##` 标题切分，每块带父标题上下文 | 开发规范、架构设计文档 |
| **会议** | 会议纪要、周报 | 按段落语义聚合，识别发言人/时间戳边界 | 周例会纪要、晨会记录 |
| **表格** | Excel/在线表格 | 按行分组，表头行作为每块前缀 | 测试用例表、需求矩阵 |
| **文本** | 无结构纯文本 | 滑动窗口，≈500 tokens/块 | 公告、说明文档 |

分类依据：标题关键词（"会议纪要""周报"） + 内容特征（表格管道符 `|` 密度）。**这个分类决策本身不需要 AI**，靠规则即可，快速且稳定。

### 2.4 向量嵌入：让机器"理解"文档语义

使用 **SentenceTransformers** 加载 `BAAI/bge-small-zh-v1.5` 对每个分块做向量化，存储到 **pgvector**。

| 选型考量 | 说明 |
|---------|------|
| 为什么 bge-small-zh | BAAI 的中文优化模型，512 维，在中文语义理解上表现优异 |
| 为什么本地部署 | 零 API 成本、零网络延迟、数据不出内网 |
| 为什么 pgvector | 和 PostgreSQL 天然集成，cosine 距离查询，不需要额外运维一套向量数据库 |

### 2.5 混合检索：两种搜索方式的互补

这是 RAG 最关键的工程决策——**单一检索方式有致命盲区**：

- 关键词匹配（MySQL FULLTEXT）：搜"路径规划"找不到"轨迹生成"，搜不到同义词
- 语义匹配（pgvector cosine）：可能被不相关的文本干扰，偏离精确的技术术语

所以采用**双路召回 + RRF 融合**：

```
用户查询
    ├── FULLTEXT 关键词匹配 → 结果集 A（精确命中）
    └── cosine 语义匹配     → 结果集 B（语义相关）
              ↓
    RRF 融合：score = Σ 1/(60 + rank_i)
              ↓
           最终 Top-K
```

**RRF 的核心价值**：不关心原始分数大小（FULLTEXT 的 relevance score 和 cosine 距离量纲完全不一样），只看排名位置。因为检索本质是排序问题不是打分问题。

### 2.6 知识库被哪些功能使用

| 功能 | 如何用到知识库 | 效果 |
|------|--------------|------|
| **任务分析** | LLM 从任务列表提取关键词 → 混合检索 → 注入分析 prompt | 建议不再是泛泛而谈，而是结合公司技术规范 |
| **AI 聊天问答** | 用户提问 → 混合检索 → 作为上下文注入 LLM → SSE 流式返回 | "XX 模块的设计规范"能直接回答 |
| **MCP 工具** | `search_knowledge_base` 工具给 Claude Code 调用 | 开发者在终端让 AI 查公司文档 |

---

## 三、任务多维度分析

这是知识库 RAG 的**第一个杀手级应用**——不只是"查文档"，而是用 RAG 增强 LLM 的分析能力。

### 3.1 核心问题

一个研发人员当前有 20+ 个 TB 任务，分布在不同的业务方向。管理者想知道：这些任务合理吗？有没有遗漏？该不该调整优先级？这些问题靠人逐个看任务列表效率太低。

**传统 BI 做法**：统计多少任务、多少工时、多少逾期。数据有了，但缺乏"洞察"。

**我们的做法**：把用户的任务数据 + 公司知识库 + LLM 的分析能力结合起来，生成结构化的改进建议——每一条建议都附带可执行的任务模板。

### 3.2 关键创新 1：LLM 自动提取关键词，连接"任务世界"和"知识世界"

这不是简单的 TF-IDF 分词。LLM 被赋予了语义理解能力——它能读懂"到点自学习精度优化"这条标题，知道要提取"自学习""到点精度"作为搜索词，而不是机械地按词频统计。

**为什么用 LLM 而不是传统 NLP？** 任务标题是研发人员写的自然语言，充满领域缩写和组合词（"叉车轨迹整体补偿方案设计"）。传统分词会拆成碎片，LLM 能保留完整的语义单元。

提取规则（通过 skill 文件定义，不硬编码）：
- 从 30 条任务标题中提取 10-15 个技术关键词
- 2 字以上，优先高频跨任务词
- 忽略通用动词（"开发""测试""优化"）
- temperature=0.1 保证结果稳定

这些关键词进入混合检索，从知识库拉回最相关的技术规范、设计文档、历史经验——它们成为 LLM 生成建议时的"领域知识"。

### 3.3 关键创新 2：Skills 驱动的分析模板，可编辑不硬编码

分析报告的 4 个模块、每个模块的输出结构、JSON 格式规范，全部定义在一个 Markdown 文件里（`SKILL.md`）。代码只负责：
1. 查数据、查知识库
2. 把数据填入模板的 `{task_context}` `{work_hour_stats}` 等占位符
3. 调 LLM，LLM 按模板规范生成 JSON

这意味着：**加一个分析维度、改建议的优先级评分规则、调输出格式，只需要改 Markdown 文件，不需要动代码、不需要重新部署。**

### 3.4 关键创新 3：两阶段 LLM 调用 + 自动容错

单次调 LLM 生成全部 4 模块报告（需求雷达 12 条 + 自主型建议 12 条 + 能力型建议 12 条 + 风险分析），输出 JSON 太长，经常被截断。我们的解法不是"换更好的模型"，而是**把问题拆开**：

| 调用 | 内容 | 模型 |
|------|------|------|
| 第 1 次 | 需求雷达 + 自主型建议 + 能力型建议 | MiniMax-M2.7 |
| 第 2 次 | 任务风险分析 | MiniMax-M2.7 |

两次调用共享同一份上下文（任务数据 + 知识库切片），只是 prompt 的模块范围不同。

**容错策略**：
- JSON 截断自动修复：统计缺失的 `}` `]` 数量，补全
- 空结果重试：某个模块 LLM 返回空？单独用该模块的 prompt section 再调一次
- reasoning_content 兜底：DeepSeek 模型的思考链也可作为输出

### 3.5 输出与闭环

最终输出 4 个模块的结构化 JSON：

| 模块 | 产出 | 亮点 |
|------|------|------|
| 需求雷达 | 12 条技术/业务需求方向 | 基于任务标题识别用户的技术领域，结合知识库判断方向合理性 |
| 自主型建议 | 12 条改进建议 + 任务模板 | 每条含来源任务、问题、优先级、工时预估、可直接创建的任务模板 |
| 能力型建议 | 12 条学习/成长建议 + 任务模板 | 不是空洞的"你应该学 XX"，而是附带强制产出要求 |
| 风险分析 | 跨任务的逾期/过载/阻塞识别 | 不是单任务视角，而是综合多个任务的关系 |

**闭环**：每条建议附带 `task_template`（标题 + 需求描述 + 产出列表），用户点"应用建议"后一键填充到 TB 任务创建面板。分析结果不是"看完就完了"，而是直接能变成下一个周期的任务。

---

## 四、AI 创建 TB 任务

知识库 RAG 的**第二个杀手级应用**——AI 不是直接在空白表单上瞎填，而是理解了用户的工作习惯和公司规范后再做建议。

### 4.1 核心问题

填一个 TB 任务单需要：写标题（≤18 字）、选工时类型、写需求描述（背景+目标）、规划产出（每项含预估天数且总和须在固定档位）、设起止日期。这些规则多、格式要求严格，新人容易填错。而 AI 如果不知道用户的习惯，建议的产出也不靠谱。

### 4.2 关键创新 1：用户历史任务作为 AI 的"上下文"

在开始创建前，AI 先做一件事：**读取用户已有的任务上下文**。

这不是简单的"列出你之前做过什么"，而是从数据库中提取用户的：
- 常用任务标题风格和术语
- 历史产出的命名方式和粒度
- 父子任务关系（这个任务可能是某个大任务的子任务）
- 常见工时类型分布

这些信息组装成 `AI_TASK_CONTEXT.md` 文件，成为 AI 做建议时的参考。本质上是 **RAG 的另一种形式**——用户的个人数据就是检索的"知识源"，只不过不需要向量检索，直接查数据库即可。

### 4.3 关键创新 2：AI 自动生成产出，不是简单填空

传统表单：用户自己脑补"产出 1 做什么(几天)、产出 2 做什么(几天)"。

AI 做法：用户只需描述"我要做什么、为什么做"，AI 参考历史任务风格 + SKILL 模板中的产出规范，自动生成建议的产出清单。**产出的天数和不是随意写的**——必须落在固定档位 [0.2, 0.5, 1.0, 1.5, 2.0, 2.5, 3.0]，这是 TB 系统的规则，AI 通过 Domain Rules 文件学到了这个约束，会自动校验。

### 4.4 关键创新 3：Skills 规则文件驱动 AI 行为

AI 创建任务时遵循的所有行为约束，都来自一个 Markdown 文件（`2_tb_create/SKILL.md`），例如：

- "一问一答，每次只问一个问题"（防止 AI 一次性甩 5 个问题）
- "产出清单生成完后统一征求确认"（防止 AI 反复打断用户）
- "参与度必须在 [0.2, 0.5, 1.0, 1.5, 2.0, 2.5, 3.0] 中，不在档位上必须提醒用户调整"
- "先检查 draft_changed.flag 再覆盖 draft.json"（防止 AI 覆盖用户的手动修改）

这些规则如果写死在 Python 代码里，改一个交互流程就要改代码部署。放在 Markdown 文件里，运维人员可以直接改、直接生效。

### 4.5 关键创新 4：draft.json 双向同步——人机协作

AI 写草稿不是直接调钉钉 API 创建，而是先写入 `draft.json` 文件。前端右侧面板实时读取展示，用户可以手动修改任何字段。

**双向同步机制**：
- 用户在面板上改了什么 → 系统写 `draft_changed.flag` → AI 下次动作前必须 `cat draft.json` 同步最新内容
- AI 修改了什么 → 写入 `draft.json` → curl 通知后端 → 前端面板刷新

这意味着 **AI 和人工不是二选一，而是协作**——AI 生成初稿，人工微调，确认后一键创建。降低填单门槛的同时保留人的最终控制权。

### 4.6 两条入口

| | Web 路径 | MCP 路径 |
|---|---------|---------|
| **场景** | 浏览器打开系统，点"AI 创建任务" | 开发者在 Claude Code 终端里说"帮我建个单子" |
| **AI 在哪里跑** | ttyd 终端中的 Claude CLI | Claude Code 直接调 MCP Tool |
| **草稿可见性** | 右侧面板可视化，可手动编辑 | 终端文本确认 |
| **创建时机** | 用户在面板点确认 | AI 在终端确认后直接调 API |

---

## 五、MCP 服务

如果说 Web 前端的任务分析和任务创建是"系统内的 AI 能力"，那 MCP 就是**把这些能力开放给开发者的终端**。

### 5.1 从"操作手册"到"原生能力"

**ttyd 模式**（Web 路径）：

> 给 Claude CLI 发一份 Markdown 操作手册 → 它按手册一问一答 → 手动 curl 通知后端

本质是"给 AI 一本说明书"。问题：操作手册占 token、curl 易出错、流程僵化。

**MCP 模式**：

> Claude Code 启动时自动发现 Server 的 Tools / Resources / Prompts → 需要时直接调用 → 自主决策

本质是 **"AI 获得了原生能力"**。不再需要"你按这个流程做"，而是"你有这些能力，你觉得怎么做最好"。

### 5.2 三种能力

| MCP 能力 | 类比 | 我们的实际内容 |
|---------|------|--------------|
| **Tools** | 给 AI 可调用的函数 | 查任务上下文、搜知识库、查工时、建 TB 任务 |
| **Resources** | 给 AI 可查阅的知识 | TB 字段规范、工时规则、任务分析/创建/问答的 skill 流程 |
| **Prompts** | 给 AI 的预设角色 | "你是 TB 任务创建助手，请按以下规则行事..." |

**关键点**：Resources 里的 skill 文件和 Web 路径用的是同一套 Markdown。一处编写，两处生效。

### 5.3 部署

```
开发者电脑 (Claude Code)  ←── SSE :5200 ──→  服务器 (FastMCP + uvicorn)
         ?user_name=张三                           独立进程，与 Flask 解耦
```

客户端只需配置一个 JSON 文件：
```json
{
  "mcpServers": {
    "tb-mcp": {
      "type": "sse",
      "url": "http://172.19.3.79:5200/sse?user_name=张三"
    }
  }
}
```

多用户通过 URL 参数区分，服务端 `contextvars` 保证请求间身份隔离。

---

## 六、总结

```
                        知识库 RAG (全文 + 语义 + RRF)
                       /              |              \
                      /               |               \
              任务分析              TB 创建            MCP 服务
         LLM提取关键词→检索    AI读用户历史→自动     Tools/Resources/
         →LLM生成建议报告      生成产出→人机协作      Prompts→终端调用
             ↓                    ↓                    ↓
        建议附带任务模板        draft.json草稿         Claude Code
        一键创建TB任务        面板确认→钉钉API        直接调用工具
```

三个应用都建立在同一套知识库 RAG 之上，共享同一套 Skills 规则文件驱动 AI 行为。核心思路始终不变：**让 AI 读懂公司文档、理解业务规则、真正帮员工做事**，而不是简单的"搭一个聊天机器人"。

---

| 层级 | 技术 |
|------|------|
| LLM 网关 & 模型 | one-api / MiniMax-M2.7、DeepSeek-v4-pro |
| AI 协议 | MCP (Model Context Protocol) |
| 嵌入模型 | BAAI/bge-small-zh-v1.5 (SentenceTransformers) |
| 向量存储 & 检索 | pgvector + MySQL FULLTEXT → RRF 融合 |
| 文档分块 | 4 策略自适应（结构化/会议/表格/文本） |
| Web 框架 & 终端 | Flask + React / ttyd + Claude CLI |
| 外部 API | 钉钉知识库 API、钉钉 TB API |
